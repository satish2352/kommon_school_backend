# Problem Statement

<!--
Replace the content below with the problem you want the standalone Python
orchestrator (orchestrator.py at repo root) to solve. The developer agent
receives this file verbatim as its first prompt.

Guidelines:
- Be explicit about inputs, outputs, constraints, and edge cases.
- Mention any files/modules the change should touch.
- If this is a bug fix, include the failing behavior and expected behavior.

This file is NOT used by the Claude Code `Task(subagent_type="orchestrator")`
flow. It is only read by orchestrator.py when run directly via
`python orchestrator.py`.
-->

_(No problem statement yet. Write one above this line before running `python orchestrator.py`.)_
